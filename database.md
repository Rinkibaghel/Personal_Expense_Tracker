DataBase --Expense_Tracker--(main database)
    |-main tables:
        |-user profile (login or signup store data)
            |-user name,mail_id,contect num,app lock password
        |-bank_accounts (auto creation tables )
            |-bank_name,bank_account_no.,current_amount,date,time,expense
        |-loans_accounts(auto creation tables)


